{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Babel",
            "type": "shell",
            "command": "${workspaceFolder}/node_modules/.bin/babel ${workspaceFolder}/src --out-dir ${workspaceFolder}/lib",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}